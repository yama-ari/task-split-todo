<%= turbo_stream.prepend "incomplete_tasks", partial: "tasks/components/task_row", locals: { task: @task } %>

<%= turbo_stream.replace "new_task_form", partial: "tasks/components/form", locals: { task: Task.new } %>
